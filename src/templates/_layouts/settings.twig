{% extends '@fragments/_layouts/base.twig' %}

{% set title = "Settings"|t('fragments') %}
{% set selectedSubnavItem = 'settings' %}
{% set selectedSidebarNavItem = (selectedSidebarNavItem is defined) ? selectedSidebarNavItem : 'settings' %}

{% block sidebar %}
    {{ include('@fragments/_includes/sidebar/settings.twig', {
        selectedSidebarNavItem: selectedSidebarNavItem
    }) }}
{% endblock %}
