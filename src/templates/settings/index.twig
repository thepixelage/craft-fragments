{% extends '_layouts/cp.html' %}
{% set title = 'Settings'|t('fragments') %}
{% set selectedSubnavItem = 'settings' %}

{% set crumbs = [
    {
        label: 'Fragments'|t('fragments'),
        url: cpUrl('fragments'),
    },
] %}

{% block sidebar %}
    {% set selectedSettingsNavItem = selectedSettingsNavItem is defined ? selectedSettingsNavItem : '' %}
    <nav>
        <ul>
            <li>
                <a href="{{ cpUrl('fragments/settings/types') }}" class="{{ selectedSettingsNavItem == 'types' ? 'sel' : '' }}">Fragment Types</a>
            </li>
            <li>
                <a href="{{ cpUrl('fragments/settings/zones') }}" class="{{ selectedSettingsNavItem == 'zones' ? 'sel' : '' }}">Zones</a>
            </li>
        </ul>
    </nav>
{% endblock %}
