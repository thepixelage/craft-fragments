{% if craft.fragments.fragmentTypes.getFragmentTypeCount() > 0 %}

    {% redirect cpUrl('fragments/fragments') %}

{% endif %}

{% if craft.fragments.zones.getZoneCount() > 0 %}

    {% redirect cpUrl('fragments/zones') %}

{% endif %}

{% redirect cpUrl('fragments/settings') %}
